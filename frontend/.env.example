VITE_API_BASE_URL=http://127.0.0.1:8000
# Opcional: estilo MapLibre propio. Si se omite, se usa OpenFreeMap Positron por defecto.
# VITE_KALMIO_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/positron
# Para pruebas desde Cloudflare Tunnel (frontend y backend en dominios distintos),
# usar el endpoint público del backend:
# VITE_API_BASE_URL=https://TU_BACKEND_TUNNEL.trycloudflare.com
